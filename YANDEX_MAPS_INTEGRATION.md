# Интеграция Яндекс Карт для выбора адреса доставки

## ✅ Реализовано

### 1. Установка зависимостей
- Установлен пакет `@yandex/ymaps3-types` для TypeScript типов
- Настроен `tsconfig.json` для поддержки типов Яндекс Карт

### 2. Конфигурация
- Добавлен API ключ Яндекс Карт в `nuxt.config.ts`: `51d550e0-cf8f-4247-bae5-dfd32b51048d`
- Добавлен скрипт загрузки Яндекс Карт API в `<head>`

### 3. Компонент AddressMapPicker
Создан компонент `components/AddressMapPicker.vue` с функциональностью:
- ✅ Открытие модального окна с картой
- ✅ Поиск адреса по тексту
- ✅ Выбор адреса кликом на карте
- ✅ Получение адреса по координатам через HTTP Geocoder API
- ✅ Отображение выбранного адреса

### 4. Интеграция в checkout.vue
- ✅ Заменен простой input на компонент `AddressMapPicker`
- ✅ Добавлен автоматический расчет доставки при выборе адреса
- ✅ Убран ручной выбор зоны доставки (теперь определяется автоматически)
- ✅ Отображение информации о зоне доставки после выбора адреса

### 5. Удаление вкладки "Зоны доставки"
- ✅ Убрана вкладка из списка вкладок админ-панели
- ✅ Удален связанный код отображения зон в админке
- ✅ Зоны доставки теперь управляются через отдельную страницу `/admin/delivery-zones/`

## 🔧 Как это работает

1. **Выбор адреса:**
   - Пользователь нажимает кнопку "Указать адрес доставки"
   - Открывается модальное окно с картой Яндекс
   - Пользователь может:
     - Ввести адрес в поле поиска
     - Кликнуть на карте для выбора точки

2. **Автоматический расчет доставки:**
   - При выборе адреса отправляется запрос на `/api/delivery-zones/calculate`
   - API определяет, в какую зону доставки попадает адрес
   - Автоматически рассчитывается стоимость доставки
   - Отображается информация о зоне (название, время доставки, минимальная сумма)

3. **Валидация:**
   - Если адрес не входит в зону доставки, показывается предупреждение
   - Заказ нельзя оформить без выбранного адреса и определенной зоны

## 📝 API Endpoints

- `POST /api/delivery-zones/calculate` - Расчет доставки по координатам
  - Body: `{ lat: number, lng: number, subtotal: number }`
  - Returns: `{ zone: {...}, deliveryPrice: number, ... }`

## 🗺️ Управление зонами доставки

Зоны доставки теперь управляются через отдельную страницу:
- `/admin/delivery-zones/` - Список зон
- `/admin/delivery-zones/new` - Создание новой зоны
- `/admin/delivery-zones/[id]` - Редактирование зоны

## 🔑 API Ключ

API ключ Яндекс Карт: `51d550e0-cf8f-4247-bae5-dfd32b51048d`

Документация: https://yandex.ru/maps-api/docs/js-api/common/connection/typescript.html

