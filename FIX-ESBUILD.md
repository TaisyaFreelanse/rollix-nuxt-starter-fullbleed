# 🔧 Исправление ошибки esbuild на Render

## Проблема:
Конфликт версий esbuild в tsx при сборке на Render.

## Решение:

### 1. Обновлен buildCommand

Build Command теперь не использует tsx при сборке (tsx нужен только для seed, который выполняется отдельно).

### 2. Обновлена версия tsx

Версия tsx обновлена до более стабильной.

### 3. Что нужно сделать:

1. **Обновите Build Command на Render:**
   - Откройте: https://dashboard.render.com/web/srv-d4mqj16mcj7s73cgv21g/settings
   - Найдите "Build Command"
   - Замените на:
     ```
     npm ci && npx prisma generate --schema=prisma/schema.prisma && npm run build
     ```
   - Сохраните

2. **Закоммитьте изменения:**
   ```bash
   git add package.json package-lock.json
   git commit -m "Fix esbuild version conflict in tsx"
   git push origin main
   ```

3. **После успешного деплоя выполните миграции:**
   - Откройте Shell на Render
   - Выполните:
     ```bash
     npx prisma db push
     npx tsx prisma/seed.ts
     ```

## Альтернативное решение (если проблема сохраняется):

Если ошибка продолжается, можно использовать более старую версию tsx:

```bash
npm install tsx@4.19.1 --save-dev
```

Или использовать node напрямую для seed (после компиляции TypeScript).

