export default defineNuxtRouteMiddleware(async (to, from) => {
  // Middleware ничего не делает - вся логика авторизации в layouts/admin.vue
  // Это позволяет избежать проблем с редиректами и состоянием
})
