# Настройка синхронизации меню с iikoCloud

## ✅ Что уже настроено

1. **Автоматическая синхронизация при старте приложения**
   - При запуске сервера меню из iikoCloud автоматически синхронизируется
   - Запускается через 5 секунд после старта (не блокирует запуск)

2. **Ручная синхронизация**
   - Команда: `npm run iiko:sync`
   - Можно запускать вручную в любое время

## 🚀 Как использовать

### Автоматическая синхронизация

Просто запустите сервер:
```bash
npm run dev
# или
npm start
```

Синхронизация запустится автоматически через 5 секунд после старта.

### Ручная синхронизация

Если нужно обновить меню вручную:
```bash
npm run iiko:sync
```

## ⚠️ Ошибка 429 (Too many requests)

Если вы видите ошибку `429: Too many requests`, это значит:
- Превышен лимит запросов к iikoCloud API
- Подождите 1-2 минуты и попробуйте снова
- Или просто перезапустите сервер - синхронизация запустится автоматически

## 📊 Проверка синхронизации

После синхронизации вы увидите статистику:
```
✅ Синхронизация завершена!
📊 Статистика:
   - Категорий синхронизировано: X
   - Товаров синхронизировано: Y
   - Цен обновлено: Z
```

## 🔍 Проверка через API

Можно проверить статус синхронизации через API:
```bash
# Статус синхронизации
curl http://localhost:3000/api/aiko/sync-status

# Health check
curl http://localhost:3000/api/aiko/health
```

## 📝 Важно

- Товары теперь загружаются **только из iikoCloud**, а не из админ-панели
- Админ-панель больше не используется для создания товаров
- Все товары синхронизируются из iikoCloud автоматически

